{
  "permissions": {
    "allow": [
      "Bash(git push *)"
    ]
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git diff --quiet && git diff --cached --quiet || (git add -A && git commit -m 'auto: sync changes to main' && git push origin HEAD:main) 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
